jobs:
  test:
    executor:
      name: node/default
      tag: '15.3.0'
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          command: yarn test
          name: Run tests
orbs:
  node: circleci/node@4.1
version: 2.1
workflows:
  workflow:
    jobs:
      - test